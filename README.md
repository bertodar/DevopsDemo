# DevOpsDemo

Sample project for DevOps Course @ ZHAW.

Commands:
@Test
void additionalSimpleTest() {
    assertEquals(4, 2 + 2);
}

git add .
git commit -m "Add additional Jenkins demo test"
git push